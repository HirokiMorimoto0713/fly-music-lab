# Fly Music Lab

日本語の神経音楽実験アプリ。`npm start` でローカル起動、`npm test` で検証。実データは `npm run prepare:data` で準備する。

神経計算・実配線・人工的な音楽と身体の変換を区別する。乱数だけの音や身体を神経出力と表示しない。データの削減や実測していない学習能力を暗黙に導入しない。上流のライセンスと出典を保持する。

メロディー実験は固定の実配線と追加の学習済み読み出し器を区別する。返事の推定に正解音符、曲ID、入力列を渡さない。学習例への適合と未知入力への一般化を区別して記録する。3Dの演技は音の再生に同期した表現で、神経の自然な運動や理解の証拠と扱わない。
