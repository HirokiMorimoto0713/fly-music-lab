# 実行記録

## 計画

- 実データの準備と来歴固定 → 決定性のあるLIF計算 → 音と身体の接続 → 日本語UIと比較実験 → WAV/MIDI/JSON → 実ブラウザ検証
- ローカル静的アプリ。ビルド依存を設けず標準ES modules、Web Worker、Web Audio、Canvasを使う。既存環境への依存追加なし。
- 学習前の実験を対象とし、音楽変換・身体の演出を生物学的再現と混同しない。
- 動作検証の完了まではgoalを完了扱いにしない。

## 状態

- 計画とgoal設定済み
- 新規Gitリポジトリを作成、既存の同名ディレクトリがないことを確認
- MaleCNS公式サイトとXenova配布物のLIF実装・データmanifestを確認
- データ準備完了: 圧縮76MB、28ファイルを固定版から取得しハッシュ検証。上流MITとデータCC BY 4.0を保持
- アプリ実装完了: 全配線LIF / 刺激 / 音・身体 / 実活動表示 / 条件保存と再実行 / 比較 / WAV・MIDI・JSON
- 4件の自動検証合格、Chromeの全ユーザーフロー検証合格。証拠は `docs/verification.md` と `artifacts/browser-results.json`
- 1280×800で開始操作が見えるよう画面密度を調整。保存欄・実習欄・モバイルを目視確認
- 操作ガイド、科学的境界、変換式、ソースの読み順を記載
- ポート4317は使用中のため変更せず、空いていた4389を選択。サーバーは127.0.0.1のみ
- 最終コード点検で準備中の停止による旧条件の上書きを修正。worker異常の再試行経路も整備
- 日本語の説明、キーボード操作、native入力、フォーカス、読み込み・エラー表示を点検。全ソースを整形して読みやすくした
- 追加ブラウザ検証合格: 中断時に元のseedを保持 / 刺激ゼロで無音 / 通常128ステップの完走（101326発火・605音）
- HTTP 200とタイトルを再確認。`npm start` のローカルサーバーが4389で稼働
- 必須実装と検証は完了。公開デプロイ、Macの実接続、学習機能は今回の対象外


## メロディー模倣への改修

- ユーザー指定: 計画→goal設定→実装。途中で3Dヘッドフォンをつけた3Dハエを追加依頼。goalを実際に設定済み。
- 固定LIFの入力後状態から、追加の線形読み出し器が3〜5音の音程と長さを推定する。未知曲と乱数変更の失敗も結果として表示。
- 同じ乱数での復習、異なる乱数、未知曲、伝播なし、入力後リセットの実データ検証。推定関数に正解・ID・長さ・時計を渡さない。
- 初回の実データ試験: 学習8曲への適合100%。のぼる/seed2で音程33%、未知曲の指定例0%。成功を一般化と解釈しない。
- Three.jsは公式npm配布とmrdoob/three.js来歴を確認。0.186.0を固定、--ignore-scriptsで取得、依存1件・audit 0。新規依存は3Dに限定し、package-lockを保存。既存の学習/計算に外部AI・資格情報は不要。
- 前の自由演奏をfree.htmlへ保存。神経データ、元のコード、元の検証は維持。
- ローカルの4389サーバーが終了していたため、同一127.0.0.1のポートでuserのtransient systemdサービスfly-music-labを起動。自動再起動あり、OS起動時の永続enableはしていない。
- 完了: npm test 6件、メロディーChrome E2E（ノート・出力・中断・欠損データ復旧・3Dなし）合格、自由演奏回帰合格、3Dの静止/聴く/停止の描画を確認。PC/モバイルを目視、文書・差分点検済み。
- 順次更新: 完成コードを既存の非公開GitHubへ反映する。新しい目標として追加された3Dハエも実装と描画検証を完了。
- 主張の境界: 学習するのは外部読み出し器。生物学的な理解や一般化成功を主張しない。今回のMac実機での音質・3D評価は未実施。

## 脳の反応画像（2026-09-11）

実計算の区間別発火とMaleCNS細胞体位置をCanvasで重ねる反応図を追加。各音と入力停止後の選択、条件比較からの表示、PNG保存に対応。表示のみ背景を約1/20、活動細胞を発火数上位500に制限し、位置不明件数を表示。全配線の計算と読み出しは維持。7件のテストとメロディー実験E2E合格、反応図のスクリーンショットを目視確認。

## お絵かき実験の計画（2026-09-12）

draw.htmlにお手本作成→追加読み出し学習→局所視覚と全配線計算を介したペン制御→比較・保存の一巡を実装する。丸・三角・波線、自作一筆線、未練習の星を用意。学習中のみ教師方向を使用し、推論には神経特徴だけを渡す。8種類のLC神経への人工入力、各観測20 msの初期化、開始点と開始方向の補助は明示する。反応図・3Dのペン・局所視野を表示。既存音楽と自由演奏を保護。実配線の境界・再現性・対照とブラウザ一巡を検証し、実測した成績と限界を記録する。

実装・検証済み: 全配線の視覚→ペンの閉ループ、鉛筆の3D、プリセットと自作線、一筆履歴と脳反応、3条件比較、PNG/JSON/ノート保存復元。9件の自動テスト、お絵かきE2E（再検証含む）、既存メロディーE2Eが合格。最終のファイル選択→復元→描画とモバイル保存欄、1280×800の開始操作を追加確認。最終画面を目視し、ガイドとREADMEに使い方を記録。初期LC4分割から8種類LCへの変更理由と実測値はガイド・検証記録に記載。Mac実機・Safariは未検証。

ローカル配信: draw.html / ガイドともHTTP 200、fly-music-lab.serviceはactive。Gitの差分確認後、既存privateリポジトリへ反映する。

## 神経出力の字幕による会話（2026-09-12）

ユーザーが採用した「言葉→目印→脳出力→根拠つきの字幕」をtalk.htmlへ実装。LC9に人工視覚入力、DNa02左右とDNp09の平均発火率から固定規則で字幕と動きを出す。字幕にはユーザー文・目印座標を渡さない。送信間は神経状態を保持して休止し、次の合図から続ける。未対応の日本語を拒否し、否定命令を誤って肯定へ変換しない。途中休止、新しいセッション、条件変更、反応履歴、JSON保存を備える。

実配線で通常の呼びかけに左→前進の出力が出ること、入力停止後の残存活動、再現性、伝播なしと無入力での無反応を確認。npm testの11件が合格。会話の実ブラウザで入力→字幕→状態継続→保存→休止再開→条件変更→対照→エラー復旧とモバイル幅を確認。ガイドに人工的な解釈・閾値・状態保持と自然な意思の区別を記録。公開サービスへの展開は行わず、既存のローカル配信とprivate GitHubに反映する。
