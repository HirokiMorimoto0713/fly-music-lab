# 実行記録

## 計画

- 実データの準備と来歴固定 → 決定性のあるLIF計算 → 音と身体の接続 → 日本語UIと比較実験 → WAV/MIDI/JSON → 実ブラウザ検証
- ローカル静的アプリ。ビルド依存を設けず標準ES modules、Web Worker、Web Audio、Canvasを使う。既存環境への依存追加なし。
- 学習前の実験を対象とし、音楽変換・身体の演出を生物学的再現と混同しない。
- 動作検証の完了まではgoalを完了扱いにしない。

## 状態

- 計画とgoal設定済み
- 新規Gitリポジトリを作成、既存の同名ディレクトリがないことを確認
- MaleCNS公式サイトとXenova配布物のLIF実装・データmanifestを確認
- データ準備完了: 圧縮76MB、28ファイルを固定版から取得しハッシュ検証。上流MITとデータCC BY 4.0を保持
- アプリ実装完了: 全配線LIF / 刺激 / 音・身体 / 実活動表示 / 条件保存と再実行 / 比較 / WAV・MIDI・JSON
- 4件の自動検証合格、Chromeの全ユーザーフロー検証合格。証拠は `docs/verification.md` と `artifacts/browser-results.json`
- 1280×800で開始操作が見えるよう画面密度を調整。保存欄・実習欄・モバイルを目視確認
- 操作ガイド、科学的境界、変換式、ソースの読み順を記載
- ポート4317は使用中のため変更せず、空いていた4389を選択。サーバーは127.0.0.1のみ
- 最終コード点検で準備中の停止による旧条件の上書きを修正。worker異常の再試行経路も整備
- 日本語の説明、キーボード操作、native入力、フォーカス、読み込み・エラー表示を点検。全ソースを整形して読みやすくした
- 追加ブラウザ検証合格: 中断時に元のseedを保持 / 刺激ゼロで無音 / 通常128ステップの完走（101326発火・605音）
- HTTP 200とタイトルを再確認。`npm start` のローカルサーバーが4389で稼働
- 必須実装と検証は完了。公開デプロイ、Macの実接続、学習機能は今回の対象外
