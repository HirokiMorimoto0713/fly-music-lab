# 検証記録

2026-09-11、母艦 motoha-EliteMini-Series で実施。生物学的妥当性や作曲学習の検証ではなく、アプリが説明通りの計算・保存・出力をする検証です。

## 自動検証

`npm test` — 4テスト合格。

- 無刺激で発火・音・動きを捏造しない
- 実験JSONの往復と不正値の拒否
- WAV/MIDIのヘッダー、波形、音符と時間情報
- 実際の全166,700神経・25,582,938接続をロードし、同一シードの一致、異なるシードの差、伝播あり／なしの差を確認

伝播対照の初期4区間（20 msずつ）で、通常の発火は2157 / 2809 / 2492 / 2180。伝播なしは700 / 662 / 693 / 705。伝播なしの脳内部群は0、通常条件では発火がありました。

## 実ブラウザ

Chrome / Playwrightで実データを用いたE2Eを実施。テスト実装: `tests/browser.mjs`。

```sh
PLAYWRIGHT_MODULE=/home/motoha/.ai-agent-shared/node_modules/playwright node tests/browser.mjs
```

このパスは母艦の既存Playwrightです。別環境ではインストール済みのplaywrightのパスを指定してください。製品ランタイムにはPlaywrightは不要です。

- 全配線ロード → 開始 → 左・右・LC4刺激 → 12ステップ → 停止
- 60音を生成。累計35,221発火
- MIDIダウンロード、同じ条件で再実行したMIDIがバイト単位で一致
- WAV合成: 171,760 bytes、16bit PCMピーク5258。無音でもクリップでもない
- JSONダウンロード → 読み込み → 再計算したMIDIもバイト単位で一致
- ブラウザ保存がページ再読み込み後も存在
- 伝播比較: あり35,221発火 / なし9,454発火。左・右の直接刺激を含むため、伝播なしでも下行性の発火は残る
- 停止後に表示のステップ数と発火数が増えない
- 不正JSONは明示的に拒否。データ404は準備コマンドを表示し、同じページから再試行して復旧
- 320 / 390 / 768 / 1280pxで横はみ出しなし。1280×800で開始操作が画面内
- 日本語のガイドとモデル説明がHTTP 200
- pageerrorなし

追加の `tests/lifecycle.mjs` も合格。準備中に停止しても前の実験のシードを保持、刺激0 Hzで音符0・音声書き出し無効、通常の128ステップを最後まで実行して自動終了を確認しました。全長の実行では101,326発火・605音でした。

`artifacts/` に実行結果JSON、WAV、MIDI、実験JSON、スクリーンショットを保存（Git対象外）。視覚点検ではPCの実験画面、保存欄、実習欄、モバイル画面を確認。全ページスクリーンショットはリサイズ直後にChromeの描画欠落が出たため、保存欄と実習欄はスクロール後の実画面も別に撮影・点検しました。

## 境界

- ライブ音のハードウェア出力を人間の耳で評価したものではありません。Web Audioの動作と合成WAVの信号・形式を確認しました。
- スマホ幅はPC上の表示検証。スマホ実機での全配線の計算速度、Safariでの動作は未確認です。
- 公開デプロイ、Mac側のSSHトンネル接続、実際のREAPERへの取り込みは未実施です。MIDI形式と実際の音符イベントは検証しています。

## メロディー模倣と3Dの追加検証（2026-09-11）

`npm test` — 6件合格。既存4件に加え、メロディーの入力・採点と、実配線の読み出し学習・推論境界・対照条件を検証しました。

| 条件 | 音程の一致 | 長さの一致 | 解釈 |
| --- | --- | --- | --- |
| 固定8曲・練習と同じシード1 | 各100% | 各100% | 学習例への適合 |
| のぼる・シード2 | 33% | 100% | 違う刺激乱数への対応は弱い |
| 未学習のシ・ド（長い）・ファ、シード2 | 0% | 0% | この例では応用できなかった |
| のぼる・伝播なし | 0% | 0% | 入力群以外の特徴はゼロ、返事は無音 |
| のぼる・聴いたあとに状態をリセット | 0% | 0% | 特徴はゼロ、返事は無音 |

特徴量ゼロ時は無音にする明示的な規則があります。したがってリセット対照の無音だけを、生物学的記憶の証明と解釈しません。全配線の重みが学習の前後で不変、同じシードと入力で特徴が一致、正解を渡さない推論関数の出力が採点対象によらない、保存した係数で出力が一致することも検証しました。成果物は `artifacts/echo-model-results.json`。

### Chromeでの実操作

`tests/echo-browser.mjs` を既存のPlaywrightとPC版Chromeで実行します。

```sh
PLAYWRIGHT_MODULE=/home/motoha/.ai-agent-shared/node_modules/playwright node tests/echo-browser.mjs
```

- WebGLで立体のハエ・ヘッドフォンを表示。角度変更で描画が変化
- お手本試聴、音付き練習の中断、鍵盤入力・音の長さ変更
- 全配線ロード、8曲の学習、復習の一致率、返事の再生
- シード2・伝播なし・状態リセットの比較
- MIDI / WAV / 実験JSON / ノートを書き出し。WAVは無音でもクリッピングでもない
- ページ再読み込みとノートインポートで学習を復元
- 未学習曲が「未学習」と表示され、勝手に学習へ追加されない
- 中断しても前の学習を保持。お手本が編集中の0音でも再読み込みで学習を失わない
- 不正ノートを拒否し、既存学習を保持
- データ404時のエラー表示と同一ページでの再試行・復旧
- 3Dモジュールが使えない場合も、実配線の練習を完了できる
- 幅320 / 390 / 768 / 1280で横はみ出しなし、pageerrorなし

以前の自由演奏に対する `tests/browser.mjs` も `/free.html` を使って再実行し、全エクスポート・再現性・対照・復元・欠落データ復旧を確認しました。移動後のルートをテストで修正した以外は、元の合格条件を維持しています。

### 確認した範囲

デスクトップとモバイル幅のスクリーンショットを点検。3Dは独自メッシュとThree.js 0.186.0、依存は固定版・スクリプト無効で取得、npm auditは0件。描画はタブが隠れたとき・画面外・静止中に抑制します。

アプリとガイド、Three.jsの配信は127.0.0.1:4389でHTTP 200。`fly-music-lab.service` の一時ユーザーサービスを起動し、ターミナル終了に依存せず配信しています。OS再起動後の自動起動は設定していません。既存のMacトンネルは14389から接続できます。

今回のMac実機上での3D・音声出力を人間が評価したわけではありません。実際の音楽理解、一般化能力、神経配線自体の学習、音声ファイルからの採譜、実物のハエの聴覚・発声は未実証または対象外です。

## 脳の反応図の追加検証（2026-09-11）

`npm test` は7件合格。既知の発火数と位置情報から、総発火・活動細胞数・位置不明細胞数・発火率が一致することを追加検証しました。既存の実配線の学習・対照結果も維持しています。

`tests/echo-browser.mjs` も合格。3音と無入力の4区間を選べること、区間変更で図が変わること、PNG保存、状態リセット後の図が活動細胞0・発火0になることを確認。既存の3D・学習・保存復元・エラー復旧・モバイル幅の検証も通過しました。`artifacts/reaction-section.png` を目視し、細胞体の背景と橙色の発火位置、区間と発火数のキャプションを確認しました。

## お絵かきの追加検証（2026-09-12）

`npm test` は9件合格。実際の全166,700神経・25,582,938接続を使う閉ループを検証しました。お絵かきの追加テストは約41秒で完了しました（母艦CPU、他テストと同時実行）。

| 条件 | 線からの平均距離 | なぞれた範囲 |
| --- | --- | --- |
| 丸・シード1 | 0.8 px | 58% |
| 丸・シード2 | 0.8 px | 58% |
| 三角・シード1 | 1.0 px | 37% |
| 波線・シード1 | 1.0 px | 100% |
| 未練習の星・シード1 | 1.1 px | 24% |
| 丸・伝播なし | 描線なし | 0% |
| 丸・視覚入力なし | 描線なし | 0% |

波線の100%は、お手本の6 px以内まで描線が届いた範囲を示します。筆順や余分な線を含む形の完全一致ではありません。丸のシードを変えると神経特徴は変わりますが、この例では丸めた距離・範囲は同じでした。105個の教師観測と同じ特徴に対する方向一致は102個。自分が動いて観測する閉ループの成績と分けて記録しています。

神経配線が学習前後で不変、同一条件の軌跡が完全一致、直接入力を除いた特徴が伝播なしでゼロ、視覚入力なしで発火もゼロ、神経特徴だけを受け取る推論が別のお手本の採点に影響されない、保存・復元した読み出し器で同じ操作になることを確認しました。視野外の画素を変えても局所入力が変わらないこと、既知の完全一致線の採点、入力境界と不正ノートの拒否も検証しています。結果は `artifacts/drawing-model-results.json`。

ブラウザの検証コマンド:

```sh
PLAYWRIGHT_MODULE=/home/motoha/.ai-agent-shared/node_modules/playwright node tests/draw-browser.mjs
```

最初の実ブラウザ検証では、鉛筆を持つ3Dと角度変更、学習→波線の閉ループ描画→一筆履歴→PNG・反応PNG・実験JSON・ノート→再読み込みと軌跡一致→3条件比較→途中停止→自作線→不正ノート拒否と復旧を確認しました。描画・再学習の中断で完了済みの学習を保持。ドラッグと矢印キーの入力、320 / 390 / 768 / 1280pxの横はみ出しなし、データ404後の再試行、3Dが使えない場合の実描画も確認し、pageerrorは0件でした。

スクリーンショットで、お手本と描線、鉛筆を持つハエ、局所視野と実発火を点検しました。Mac実機やSafariでの新画面は未検証です。既存のSSHトンネルからアクセスするローカル配信を使い、公開デプロイは行っていません。

最終の表示調整後、お絵かきE2Eを再度実行し合格。共通の3D・反応図を使用する `tests/echo-browser.mjs` も合格しました。追加で1280×800の開始ボタン全体の表示、実際のファイル選択ダイアログからのノート読み込みと復元後の描画、390pxでの保存欄を確認。最終画面 `artifacts/drawing-studio.png` と `artifacts/drawing-save-mobile.png` を目視しました。draw.htmlと実習ガイドはHTTP 200、既存のfly-music-lab.serviceはactiveです。

## 会話と神経出力の翻訳（2026-09-12）

npm testの11件が合格。会話の実配線テストでは、DNa02の左右各1細胞、DNp09の2細胞を読み出しています。シード1で正面に目印を置いた16区間では、字幕は最初の3区間が「左かな」、残りが「前へ行こう」。位置は(128,188)から約(121.1,146.4)へ変化しました。すべての字幕が測定した平均発火率の翻訳と一致します。

目印を消した次の20 msでも1264発火が残り、前進の読み出しは直近60 ms平均で83.3 Hzでした。状態を維持していることを確認する例であり、意志や記憶の実証ではありません。初期状態の無入力は0発火。新しいセッションで伝播を切った場合は入力側だけが発火し、字幕は全区間「……」、読み出し・移動ともゼロ。同一シード・同一入力で全16区間の神経反応と位置が完全一致しました。

実ブラウザのtests/talk-browser.mjsも再検証して合格。未対応語句の案内、呼びかけ→字幕→状態を保持した次の合図、履歴の参照、JSON保存、途中休止→次の送信での続行、条件変更→新しいセッション→伝播なし、データ404からの再試行・復旧を確認しました。3Dモジュールが失敗した場合も会話と無入力の計算を完了できることを確認。320 / 390 / 768 / 1280pxで横はみ出しなし、pageerrorなし。

最終追加チェックで、入力欄へ実際に日本語を入力してEnterで送信し、保存JSONにデータ版・読み出し神経ID・元の文章があることを確認しました。音楽・お絵かきのナビゲーション追加後も320pxで横はみ出しなし。最終PC画面とスマホの入力欄を目視確認。talk.htmlと会話ガイドはHTTP 200、既存fly-music-lab.serviceはactive。今回Mac実機やSafariでの動作は未確認です。

結果はartifacts/talk-model-results.json、artifacts/talk-final-export.json、画面はdocs/talk-preview.png。機能は人工的な視覚前処理と字幕規則を含み、自然な日本語理解・意識・欲求の読み取り・学習を検証したものではありません。
